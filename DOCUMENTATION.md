# Документация проекта TochkaLi

## 📋 Содержание

1. [Общее описание](#общее-описание)
2. [Архитектура системы](#архитектура-системы)
3. [Технологический стек](#технологический-стек)
4. [Структура проекта](#структура-проекта)
5. [Основные функции](#основные-функции)
6. [API документация](#api-документация)
7. [База данных](#база-данных)
8. [Безопасность и авторизация](#безопасность-и-авторизация)
9. [Telegram WebApp интеграция](#telegram-webapp-интеграция)
10. [Платежная система](#платежная-система)
11. [Развертывание](#развертывание)
12. [Конфигурация](#конфигурация)

---

## Общее описание

**TochkaLi** — это веб-приложение для Telegram, предоставляющее пользователям доступ к контенту по астрологии, медитациям, практикам и видео-урокам. Приложение работает исключительно как Telegram WebApp и включает в себя:

- **Клиентское приложение** — интерфейс для пользователей в Telegram
- **Административную панель** — веб-интерфейс для управления контентом и пользователями
- **Backend API** — серверная часть на Node.js/Express
- **Telegram Bot** — бот для управления пользователями и рассылок

### Основные возможности

- 📱 Работа только через Telegram WebApp
- 🔐 Система авторизации и ролей
- 💳 Интеграция с платежной системой Robokassa
- 📅 Управление расписанием событий
- 📝 Дневник пользователя
- 🎥 Видео-уроки с отслеживанием прогресса
- 📊 Система подписок и доступа к контенту
- 👥 Реферальная система
- 📧 Email уведомления

---

## Архитектура системы

### Трехуровневая архитектура

```
┌─────────────────────────────────────────┐
│         Telegram WebApp (Client)        │
│         React + TypeScript + Vite        │
└─────────────────┬───────────────────────┘
                  │
                  │ HTTPS/REST API
                  │
┌─────────────────▼───────────────────────┐
│         Backend API (Server)            │
│      Node.js + Express + MongoDB        │
└─────────────────┬───────────────────────┘
                  │
                  │
┌─────────────────▼───────────────────────┐
│         MongoDB Database                │
│         Mongoose ODM                    │
└─────────────────────────────────────────┘
```

### Компоненты системы

1. **Client (Frontend)**
   - React 19 + TypeScript
   - Vite для сборки
   - React Router для маршрутизации
   - TailwindCSS для стилей
   - Telegram WebApp SDK

2. **Server (Backend)**
   - Node.js + Express 5
   - MongoDB + Mongoose
   - JWT для аутентификации
   - Swagger для API документации

3. **Bot**
   - Telegram Bot API
   - Управление пользователями
   - Рассылки сообщений

---

## Технологический стек

### Frontend (Client)

- **React** 19.1.1 — UI библиотека
- **TypeScript** 5.9.3 — типизация
- **Vite** 7.1.7 — сборщик и dev-сервер
- **React Router** 7.9.4 — маршрутизация
- **TailwindCSS** 4.1.14 — стилизация
- **Axios** 1.12.2 — HTTP клиент
- **React Toastify** 11.0.5 — уведомления
- **Lucide React** 0.545.0 — иконки

### Backend (Server)

- **Node.js** — серверная платформа
- **Express** 5.1.0 — веб-фреймворк
- **MongoDB** — база данных
- **Mongoose** 8.19.1 — ODM для MongoDB
- **JWT** 9.0.2 — токены аутентификации
- **Bcrypt** 6.0.0 — хеширование паролей
- **Multer** 2.0.2 — загрузка файлов
- **Node-cron** 4.2.1 — планировщик задач
- **Nodemailer** 7.0.9 — отправка email
- **Swagger UI** 5.0.1 — API документация

### Bot

- **Telegraf** — фреймворк для Telegram ботов
- **Axios** — HTTP запросы к API

---

## Структура проекта

```
tochkaLi/
├── client/                 # Клиентское приложение
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   │   ├── Admin/     # Компоненты админ-панели
│   │   │   └── User/      # Компоненты пользовательского интерфейса
│   │   ├── pages/         # Страницы приложения
│   │   │   ├── Admin/     # Страницы админ-панели
│   │   │   └── User/      # Страницы пользовательского приложения
│   │   ├── contexts/      # React контексты
│   │   ├── utils/         # Утилиты
│   │   ├── routes.tsx     # Маршрутизация
│   │   └── api.tsx        # API клиент
│   └── package.json
│
├── server/                # Backend сервер
│   ├── Controllers/       # Контроллеры (бизнес-логика)
│   ├── Models/           # Модели данных (Mongoose)
│   ├── Middlewares/      # Промежуточное ПО
│   ├── uploads/          # Загруженные файлы
│   ├── index.js          # Точка входа сервера
│   └── swagger.yaml      # API документация
│
└── bot/                  # Telegram бот
    ├── server.js         # Сервер бота
    └── bot.js            # Логика бота
```

---

## Основные функции

### Для пользователей (Client)

1. **Авторизация и регистрация**
   - Регистрация через Telegram
   - Email подтверждение
   - Восстановление пароля

2. **Контент**
   - Гороскопы (антисоциумный гороскоп)
   - Медитации
   - Практики
   - Видео-уроки
   - Планетарные транзиты
   - Часто задаваемые вопросы (FAQ)

3. **Расписание**
   - Просмотр событий
   - Фильтрация по датам
   - Добавление в календарь (Google/Apple)

4. **Дневник**
   - Создание записей
   - Просмотр истории

5. **Профиль**
   - Управление данными
   - История покупок
   - Реферальная система
   - Подписки

6. **Платежи**
   - Покупка подписок
   - Интеграция с Robokassa

### Для администраторов (Admin Panel)

1. **Управление пользователями**
   - Просмотр всех пользователей
   - Блокировка/разблокировка
   - Активация подписок
   - Экспорт в Excel

2. **Управление контентом**
   - FAQ
   - Гороскопы
   - Медитации
   - Практики
   - Видео-уроки
   - Расписание событий
   - Транзиты
   - Динамический контент

3. **Информационные страницы**
   - Приветствие
   - О клубе
   - Шуман резонанс

4. **Рассылки**
   - Массовые рассылки через Telegram бота
   - Персонализация сообщений

5. **Управление администраторами**
   - Создание/редактирование админов
   - Управление ролями

---

## API документация

### Базовый URL

```
/api
```

### Swagger UI

Документация API доступна по адресу:
```
/api/docs
```

### Основные эндпоинты

#### Авторизация и пользователи

- `POST /api/user/create` — создание пользователя
- `POST /api/user/register` — регистрация
- `POST /api/user/login` — вход
- `POST /api/user/send-mail` — отправка кода подтверждения
- `POST /api/user/code-confirm` — подтверждение кода
- `GET /api/user/me` — получение текущего пользователя
- `PUT /api/user/profile/update` — обновление профиля

#### Контент

- `GET /api/horoscope` — список гороскопов
- `GET /api/horoscope/current` — текущий гороскоп
- `GET /api/meditation` — список медитаций
- `GET /api/practice` — список практик
- `GET /api/video-lesson` — список видео-уроков
- `GET /api/schedule` — расписание событий
- `GET /api/transit/current` — текущий транзит

#### Административные

Все административные эндпоинты требуют авторизации и соответствующих ролей.

---

## База данных

### MongoDB Collections

#### User (Пользователи)

```javascript
{
  telegramId: String,
  telegramUserName: String,
  mail: String,
  password: String (hashed),
  fullName: String,
  userName: String,
  phone: String,
  role: Enum['user', 'admin', 'content_manager', 'client_manager'],
  status: Enum['guest', 'registered', 'active', 'client'],
  isBlocked: Boolean,
  products: [{
    productId: String,
    type: Enum['subscription', 'one-time', 'free'],
    paymentDate: Date,
    paymentAmount: Number,
    paymentStatus: Enum['pending', 'paid', 'failed']
  }],
  bonus: Number,
  subscriptionEndDate: Date,
  invitedUser: ObjectId (ref: User),
  inviteesCount: Number,
  profilePhotoUrl: String,
  // ... другие поля
}
```

#### Horoscope (Гороскопы)

```javascript
{
  title: String,
  startDate: Date,
  endDate: Date,
  content: String,
  image: String,
  accessType: Enum['free', 'paid', 'subscription']
}
```

#### Meditation, Practice, VideoLesson

Схожие структуры с полями:
- `title`, `description`, `content`
- `image`, `videoUrl` (для видео)
- `accessType` — тип доступа
- `order` — порядок отображения

#### Schedule (Расписание)

```javascript
{
  eventTitle: String,
  startDate: Date,
  endDate: Date,
  eventLink: String,
  googleCalendarLink: String,
  appleCalendarLink: String,
  description: String
}
```

#### Diary (Дневник)

```javascript
{
  userId: ObjectId (ref: User),
  content: String,
  date: Date
}
```

#### VideoProgress (Прогресс видео)

```javascript
{
  userId: ObjectId (ref: User),
  videoLessonId: ObjectId (ref: VideoLesson),
  progress: Number, // процент просмотра
  lastPosition: Number // последняя позиция в секундах
}
```

---

## Безопасность и авторизация

### Система ролей

1. **user** — обычный пользователь
2. **admin** — полный доступ ко всем функциям
3. **content_manager** — управление контентом
4. **client_manager** — управление пользователями

### JWT токены

- **Access Token** — короткоживущий токен для авторизации
- **Refresh Token** — долгоживущий токен для обновления access token

### Middleware

- `authMiddleware` — проверка авторизации
- `roleMiddleware` — проверка ролей (не используется в текущей версии)

### Защита Telegram WebApp

Все страницы пользовательского приложения защищены компонентом `TelegramGuard`, который проверяет:

1. Наличие `window.Telegram.WebApp`
2. Наличие `initData` (непустая строка)
3. Наличие `initDataUnsafe.user` с данными пользователя

Если приложение открыто не в Telegram, пользователь перенаправляется на страницу `/client/blocked-browser`.

---

## Telegram WebApp интеграция

### Инициализация

Telegram WebApp SDK загружается в `index.html`:

```html
<script src="https://telegram.org/js/telegram-web-app.js"></script>
```

### Основные функции

1. **Расширение на весь экран**
   - Автоматическое расширение при загрузке
   - Обработка событий `viewportChanged`

2. **Безопасная область (Safe Area)**
   - Поддержка для iOS и Android
   - CSS переменные `--tg-safe-area-*`

3. **Открытие ссылок**
   - `window.Telegram.WebApp.openLink()` — для внешних ссылок
   - Автоматическое определение контекста

4. **Кнопка "Назад"**
   - Показ/скрытие кнопки
   - Обработка нажатий

### Проверка окружения

Функция `isTelegramWebView()` проверяет:
- Наличие `initData` (непустая строка)
- Наличие `initDataUnsafe.user` с `id`

---

## Платежная система

### Robokassa

Интеграция с платежной системой Robokassa для обработки платежей за подписки.

#### Основные эндпоинты

- `POST /api/robokassa/create-payment` — создание платежа
- `GET /api/robokassa_callback/success` — успешный платеж
- `GET /api/robokassa_callback/fail` — неудачный платеж

#### Процесс оплаты

1. Пользователь выбирает подписку
2. Создается платеж через Robokassa API
3. Пользователь перенаправляется на страницу оплаты
4. После оплаты — callback на сервер
5. Активация подписки пользователя

---

## Развертывание

### Требования

- Node.js 18+
- MongoDB 6+
- Telegram Bot Token

### Переменные окружения

#### Server (.env)

```env
MONGOURL=mongodb://localhost:27017/tochkali
JWT_SECRET=your-secret-key
JWT_REFRESH_SECRET=your-refresh-secret-key
VITE_API_URL=http://localhost:5000
MailSMTP=your-email-password
BOT_SERVER_URL=http://localhost:5011
ROBOKASSA_MERCHANT_LOGIN=your-merchant-login
ROBOKASSA_PASSWORD_1=your-password-1
ROBOKASSA_PASSWORD_2=your-password-2
ROBOKASSA_TEST_MODE=true
```

#### Client (.env)

```env
VITE_API_URL=http://localhost:5000
```

### Установка и запуск

#### Backend

```bash
cd server
npm install
npm run dev  # для разработки
npm start    # для продакшена
```

#### Frontend

```bash
cd client
npm install
npm run dev  # для разработки
npm run build  # для продакшена
```

#### Bot

```bash
cd bot
npm install
node server.js
```

### Production сборка

```bash
# Client
cd client
npm run build
# Файлы в папке dist/

# Server
cd server
npm start
```

---

## Конфигурация

### Часовой пояс

Приложение использует часовой пояс **Asia/Almaty (UTC+6)** для:
- Сохранения дат событий
- Cron задач (проверка подписок в 12:00)

### Обработка дат

- **Сохранение**: datetime-local интерпретируется как время в Asia/Almaty и конвертируется в UTC
- **Отображение**: UTC время конвертируется обратно в Asia/Almaty для показа пользователю

### Cron задачи

Автоматическая проверка истекших подписок:
- Запускается каждый день в 12:00 (Asia/Almaty)
- Обновляет статус пользователей с истекшими подписками

---

## Дополнительная информация

### Структура API ответов

Успешный ответ:
```json
{
  "success": true,
  "data": {...},
  "message": "Опциональное сообщение"
}
```

Ошибка:
```json
{
  "success": false,
  "message": "Описание ошибки",
  "error": "Детали ошибки"
}
```

### Загрузка файлов

Файлы загружаются в папку `server/uploads/` через Multer middleware.

### Email уведомления

Используется Nodemailer с Gmail SMTP для:
- Отправки кодов подтверждения
- Восстановления пароля

---

## Контакты и поддержка

Для вопросов и поддержки обращайтесь к разработчикам проекта.

---

**Версия документации:** 1.0.0  
**Последнее обновление:** 2024

